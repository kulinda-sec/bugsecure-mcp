# Tool: raise_appeal (reports:write)
mutation RaiseAppeal($input: RaiseAppealInput!, $clientRequestId: String!) {
  raiseAppeal(input: $input, clientRequestId: $clientRequestId) {
    id
    reportId
    adjudicationId
    status
    createdAt
  }
}
