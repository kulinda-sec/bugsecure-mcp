# Tool: list_my_organizations (triage:read). Only the fields below: one field refused to OAuth
# clients fails the whole operation.
query ListMyOrganizations {
  myOrganizations {
    id
    name
    slug
    aiTriageAccessEnabled
    aiGradingEnabled
  }
}
