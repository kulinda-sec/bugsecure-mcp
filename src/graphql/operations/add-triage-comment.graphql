# Tool: add_triage_comment (triage:write)
mutation AddTriageComment($input: AddCommentInput!, $clientRequestId: String!) {
  addReportComment(input: $input, clientRequestId: $clientRequestId) {
    id
    reportId
    isInternal
    createdAt
  }
}
